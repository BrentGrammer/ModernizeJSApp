{
  "name": "vanillajs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "watch": "vite build --watch",
    "start": "docker compose up && npm run watch"
  },
  "devDependencies": {
    "vite": "^5.3.1"
  }
}
