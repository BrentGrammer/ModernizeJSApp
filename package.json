{
  "name": "your-app",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel watch src/**/*.js --dist-dir wwwroot/js",
    "build": "parcel build src/**/*.js --dist-dir wwwroot/js"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
