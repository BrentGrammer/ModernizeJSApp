{
  "name": "vanillajs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "watch": "vite build --watch",
    "start": "concurrently \"docker compose up\" \"npm run watch\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vite": "^5.3.1"
  }
}
